# src/main/resources/application.yaml
spring:
  application:
    name: PrintOps

  datasource:
    url: jdbc:postgresql://localhost:5432/printops
    username: juampibujaldon
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  # Clave base64 de al menos 256 bits para HMAC-SHA256
  secret: "cHJpbnRvcHMtc2VjcmV0LWtleS0yMDI0LW11c3QtYmUtYXQtbGVhc3QtMzItY2hhcnM="
  expiration: 900000           # 15 minutos en ms
  refresh-expiration: 604800000         # 7 días en ms
  refresh-expiration-remember: 2592000000  # 30 días en ms

server:
  port: 8080
